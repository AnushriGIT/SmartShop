[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = [
    "integration: requires a live PostgreSQL instance (docker compose up -d postgres)",
    "eval: requires OPENAI_API_KEY and makes real, billed OpenAI calls",
]
